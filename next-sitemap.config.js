/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.gamesonlinegratis.com', // <-- replace with your domain (no trailing slash)
  generateRobotsTxt: true, // generates robots.txt
  sitemapSize: 5000, // split if you have many URLs
};
